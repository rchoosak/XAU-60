# การวิเคราะห์กลยุทธ์ CRT + TBS อย่างละเอียด 📈

กลยุทธ์ **`crt_tbs.py`** ถือเป็นกลยุทธ์สาย ICT / SMC (Smart Money Concept) ที่มีความแม่นยำสูงมาก โดยหลักการทำงานของมันคือ **Candle Range Theory (CRT)** ผสมกับ **Time-Based Strategy (TBS)**

## 🧠 หลักการทำงาน (Logic) ของกลยุทธ์นี้

### 1. ตีกรอบราคา (Asian Range)
บอทจะคำนวณหาจุด "สูงสุด" (High) และ "ต่ำสุด" (Low) ของกราฟในช่วงเวลาตลาดเอเชีย (ค่าเริ่มต้นตั้งไว้ที่ `00:00 - 06:00` UTC) 

### 2. ดักยิงเฉพาะช่วงเวลาทอง (Killzones)
บอทจะไม่เทรดแบบสุ่มสี่สุ่มห้า แต่จะตื่นมาสแกนหาจุดเข้าเฉพาะในช่วงที่ตลาดมีวอลลุ่มหนาแน่นจากกองทุนใหญ่เท่านั้น ได้แก่:
- **ตลาดลอนดอน (London Killzone):** 07:00 - 09:00 UTC
- **ตลาดนิวยอร์ก (NY Killzone):** 13:00 - 15:00 UTC
*(โดยจะจำกัดการเปิดแค่วันละ 1 ไม้ ต่อ 1 ช่วงเวลาเท่านั้น)*

### 3. จุดรอคอย (Liquidity Sweep)
เป็นการดักรอรายย่อยโดนกิน Stop Loss ก่อน (Manipulation) 
- **ฝั่ง BUY:** รอให้ราคาทะลุทำไส้รูดลงไป **ต่ำกว่าจุด Low ของเอเชีย** (ล่อให้เม่าพากันกด Sell ตาม) จากนั้นเมื่อ **แท่งเทียนสามารถปิดกลับเข้ามาในกรอบได้** ให้ยิง BUY สวนทันที!
- **ฝั่ง SELL:** รอให้ราคาทะลุตั้งทรงหลอก **สูงกว่าจุด High ของเอเชีย** จากนั้นเมื่อ **แท่งเทียนอัดกลับปิดในกรอบได้** ให้ยิง SELL ยัดใส่ทันที!

### 4. จุดทำกำไรและปิดหนี (Exit Strategy)
- **Stop Loss (SL):** ห่างจากปลายไส้เทียนที่ทะลุไป 10 Pips (กันโดนสวิงหลอกรอบสอง)
- **Take Profit (TP):** ยิงไปที่ขอบฝั่งตรงข้ามของกรอบเอเชีย (ยิง Buy ตรงตูด ไปทำกำไรเอาตรงยอดกรอบ)

---

## 🛠️ วิธีจูนนิ่ง (Tuning) ให้ทำกำไรได้ดีที่สุด

มีจุดบอดและพารามิเตอร์ 3 จุดที่ควรปรับจูนก่อนนำไปรันจริวกับทองคำ (XAUUSD) ในไฟล์ `config/strategies/crt_tbs.yaml`:

### 1. ⚠️ ปรับแก้ TIMEZONE ของ Broker (โคตรสำคัญสุด)
ในโค้ดตั้งเวลาไว้เป็น UTC เป๊ะ แต่ **Server ของ MT5 โบรกเกอร์ทั่วไปมักจะใช้เวลาเป็น UTC+2 หรือ UTC+3**! ถ้าไม่เซ็ตค่านี้ เวลาตลาดเอเชียจะดึงกราฟผิดเวลาไปตีผิดกรอบทั้งหมด ส่งผลให้ยิงแพ้รัวๆ
> **วิธีปรับ (สำหรับโบรกทั่วไปที่ใช้ UTC+3):**
> เพิ่มบล็อก `session` ลงใน `crt_tbs.yaml` ดังนี้:

```yaml
session:
  asian:
    start_hour: 2    # (00:00 UTC = 02:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
    end_hour: 8      # (06:00 UTC = 08:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
  london_killzone:
    start_hour: 9    # (07:00 UTC = 09:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
    end_hour: 12     # (10:00 UTC = 12:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
  ny_killzone:
    start_hour: 15   # (13:00 UTC = 15:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
    end_hour: 18     # (16:00 UTC = 18:00 เวลาเซิร์ฟเวอร์โบรกเกอร์)
```

### 2. ปรับระยะ SL และ Take Profit
ทองคำ XAUUSD วิ่งแรงและสวิงกว้าง ควรกำหนดระยะเผื่อการโดนทะลุไส้อีกครั้ง
> **วิธีปรับ:** อัพเดตบล็อก `parameters` ให้เป็นแบบด้านล่าง

```yaml
parameters:
  lot_size: 0.01  
  sl_pips_beyond_sweep: 15.0   # ทองสวิงแรง ควรเปลี่ยนจาก 10 เป็น 15-20 pips เผื่อโดนทำดับเบิ้ลบอททอมกิน Stop
  use_range_tp: false          # เปลี่ยนเป็น false เถอะ เพราะถ้าวันไหนกรอบเอเชียมันแคบมาก TP เราจะไม่คุ้มเสียเลย (Risk-Reward ต่ำ)
  fixed_rr: 2.0                # เลือกใช้ R:R แบบคงที่ 1:2 แทน ปลอดภัยทางคณิตศาสตร์แบบเซฟโซน
  max_trades_per_killzone: 1
```

### 3. ตัวกรอง Timeframe
การตั้งค่า `timeframe:` ในไฟล์พารามิเตอร์ ควรใช้ **`M5`** (5 นาที) หรือ **`M15`** เป็นมาตรฐานหลัก
- ❌ `M1`: ไส้เทียนหลอก (Noise) เยอะไป จะไปรัน Trigger ให้เข้าผิดจังหวะบ่อย
- ❌ `H1`: แท่งปิดจบช้าไป กว่าจะรู้ว่า "อ้าว ดึงกลับเข้ากรอบแล้วนี่หว่า" ราคาอาจจะวิ่งหนีทะลุกรอบไปไกลจนตกขบวนหน้าแหกแล้ว

---
**✨ สรุป:** นำ Config ด้านบนนี้ไปปรับจูนใน `config/strategies/crt_tbs.yaml` จะช่วยให้กลยุทธ์ CRT มีความแม่นยำสูงขึ้นอย่างก้าวกระโดดสำหรับเทรด XAUUSD ครับ!
